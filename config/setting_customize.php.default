<?php
/**
 * BcBackup カスタマイズ設定
 *
 * このファイルを setting_customize.php にリネームして使用してください。
 * setting.php の値をここで上書きできます。
 * setting_customize.php は .gitignore に含まれているため、
 * リポジトリにコミットされません。
 *
 * 保存先フォルダ・バックアップ方式・世代管理などの運用設定は
 * 管理画面（プラグイン > バックアップ）で設定します。ここでは
 * 管理画面に出さない内部的な挙動のみ上書きできます。
 */

$customize_config = [
    'BcBackup' => [
        // 進捗通知の間隔（アーカイブ作成時、この件数ごとにステータスを更新する）
        // 'progressInterval' => 25,

        // バックグラウンド実行のロック取得を待つ猶予秒数
        // 起動が遅い環境で「異常終了した可能性があります」と誤表示される場合に延ばす
        // 'startGraceSeconds' => 30,

        // 管理画面のステータスポーリング間隔（ミリ秒）
        // 'pollingIntervalMs' => 3000,
    ],
];
